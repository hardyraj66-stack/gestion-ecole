feat(create-eleve): allow assignment to full classes with confirmation warning

Backend:
- ReadService.getClassesParNiveau: suggestion now falls back to full classes
  when all classes in a niveau are at capacity (pool = all classes if none available)
- suggestedId is always returned even when the suggested class is full

Frontend (CreateEleve):
- Removed click block on full classes (no more `!c.pleine &&` guard)
- handleSelectClasse now checks if class is full and shows a ConfirmDialog
  with variant "warning" asking user to confirm or choose another class
- Full classes show a "Complète" danger badge but remain clickable
- When a full class is selected, the selection badge shows an orange
  "Dépassement" warning badge and orange border
- Suggestion card adapts visually when the suggested class is full:
  yellow background, warning badge instead of success
- All classes in the popup list show a left orange border when full

CSS:
- Added .classe-selection-badge-warning styles (orange border/bg)
- Added .classe-popup-item-warning (orange left border for full items)
- Added .classe-popup-suggestion-warning (yellow suggestion when all full)
