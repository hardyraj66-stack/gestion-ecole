node_modules/
dist/
.DS_Store
*.log

# Variables d'environnement (SMTP, secrets) — ne jamais committer
.env
server/.env
frontend/.env

# Server
server/node_modules/
server/dist/
# Build incrémental TypeScript (ne JAMAIS committer : fausse les builds Docker/CID)
*.tsbuildinfo

# Prompts perso
prompts/

# Local Claude settings & worktrees
.claude/
# Playwright artefacts
test-results/
server/test-results/
playwright-report/
